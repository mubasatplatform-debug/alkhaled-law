name: Runner Smoke

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  smoke:
    name: runner-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Prove runner startup
        run: |
          echo "runner-started"
          uname -a
          node --version || true
